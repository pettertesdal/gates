# Application
NODE_ENV=development

# Database connection settings
DB_SERVER=db
DB_PORT=1433
DB_NAME=gates
DB_USER=gates_user
DB_PASS=GatesUser!Pass123

# SQL Server admin password used by docker-compose
SA_PASSWORD=YourStrong!Passw0rd

# Authentication tokens
NUXT_TOKEN_SECRET=gates-dev-secret
NUXT_TOKEN_EXPIRATION=12h

# Container images for the production compose profile
# APP_IMAGE=ghcr.io/<owner>/gates-app:latest
# SQL_IMAGE=ghcr.io/<owner>/gates-sqlserver:latest
